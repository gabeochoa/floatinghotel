# R7: Repository > Push
# Verifies clicking Push attempts a git push.
# Test repo has no remote, so the push will fail gracefully.
# We verify the app doesn't crash and a refresh is triggered.
make_test_repo
resize 1280 720
wait_frames 5

validate branch=main

# Push via Repository menu
click_text "Repository"
wait_frames 5
click_text "Push"
wait_for_refresh

# App should still be responsive (validate something to prove it)
validate branch=main
screenshot r7_01_push_attempted
