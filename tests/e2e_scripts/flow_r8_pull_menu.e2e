# R8: Repository > Pull
# Verifies clicking Pull attempts a git pull.
# Test repo has no remote, so the pull will fail gracefully.
make_test_repo
resize 1280 720
wait_frames 5

validate branch=main

# Pull via Repository menu
click_text "Repository"
wait_frames 5
click_text "Pull"
wait_for_refresh

# App should still be responsive
validate branch=main
screenshot r8_01_pull_attempted
