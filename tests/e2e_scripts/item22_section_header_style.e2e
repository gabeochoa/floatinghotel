# Item 22: Section headers have refined styling
# No distinct background, subtler text, tighter padding
# TODO(dump_ui): validate header bg color and font size in XML
make_test_repo
resize 1280 720
wait_for_refresh
wait_frames 30

# Section headers should still be visible with counts
expect_text "STAGED CHANGES"
expect_text "UNSTAGED CHANGES"
expect_text "UNTRACKED"

screenshot item22_section_style
