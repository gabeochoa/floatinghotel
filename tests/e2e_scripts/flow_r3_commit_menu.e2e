# R3: Repository > Commit...
# Verifies clicking Commit triggers the commit flow.
# The commit editor should become active/visible.
make_test_repo
resize 1280 720
wait_frames 5

# We have staged files, so commit should be possible
validate staged_count=1

# Trigger commit via Repository menu
click_text "Repository"
wait_frames 5
click_text "Commit..."
wait_frames 5

# The commit editor area should be visible/active
# (commitRequested is ephemeral so we check for visible UI instead)
screenshot r3_01_commit_triggered
